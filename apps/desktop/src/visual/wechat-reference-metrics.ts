/** Measured from the local WeChat 4.1.11 references; no reference pixels are shipped. */
export const WECHAT_REFERENCE_METRICS = Object.freeze({
  accent: '#07c160',
  selectionHandle: 6,
  overlayMaskAlpha: 0.3,
  toolbar: Object.freeze({
    button: 28,
    icon: 20,
    gap: 2,
    radius: 8,
    stroke: 1.8,
  }),
  scrollPreview: Object.freeze({
    desiredWidth: 172,
    minimumWidth: 120,
    gap: 6,
    action: 34,
    actionGap: 4,
    edgeAnchor: 6,
  }),
});
